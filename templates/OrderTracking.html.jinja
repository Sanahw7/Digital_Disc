<!doctype html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-teal-50">
 {% include "components/navbar.html.jinja"%}
<main class="flex flex-col-reverse justify-center pt-10">
{%for x in orders%}
<div class="flex justify-center pt-10 border-2 max-w-88 max-h-88 pt-6">
    <div class="font-bold text-lg w-xl">{{x['ID']}}</div>
   
    <div class="">{{x['Timestamp'].strftime('%m/%d/%Y %-I:%M %p')}}</div>
     <div class="font-bold">${{x["Total"]}}</div>
     <br>
     <br>
     <div class="mr-9">{{x['Quantity']}}</div>
     
            

</div>



{%endfor%}


</main>


  </body>
</html>